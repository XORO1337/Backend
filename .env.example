# Environment Configuration
NODE_ENV=development

# Server Configuration
PORT=3000

# Database Configuration
MONGO_URI=mongodb://localhost:27017/artisan_management

# Optional: For production MongoDB Atlas
# MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/artisan_management?retryWrites=true&w=majority

# JWT Configuration (if implementing authentication later)
# JWT_SECRET=your_jwt_secret_key
# JWT_EXPIRE=30d

# Other configurations
# BCRYPT_SALT_ROUNDS=10
